# Quiz (JS)

A vanilla-JavaScript quiz game — questions loaded from `questions.json`, scored as you go.

**Play:** answer each multiple-choice question; final score shown at the end.
**Tech:** vanilla JS + HTML/CSS; questions in `questions.json`.
**Run:** open `index.html` in a browser.
